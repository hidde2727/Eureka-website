CREATE TABLE IF NOT EXISTS labels_to_inspiration (
    label_id INT UNSIGNED NOT NULL PRIMARY KEY,
    inspiration_id INT UNSIGNED NOT NULL PRIMARY KEY,

    FOREIGN KEY(label_id) REFERENCES labels(id),
    FOREIGN KEY(inspiration_id) REFERENCES inspiration(id),

    INDEX(label_id)
)